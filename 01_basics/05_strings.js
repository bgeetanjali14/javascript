const name = "geet"
const repoCount = 50

// console.log(name + repoCount);

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`); //string interpolation-makes placeholders


const gameName = new String('geethc')
